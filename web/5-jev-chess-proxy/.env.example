# Copy to .env and fill in. .env is git-ignored; never commit it.
TYPESAFE_API_KEY=
# Must match the ipam subnet in docker-compose.yml (where caddy lives).
GATEWAY_SUBNET=172.30.42.0/24
# ACME contact for the Let's Encrypt cert.
CADDY_EMAIL=admin@tgr.rs
# Optional overrides (defaults shown):
#PORT=8443
#UPSTREAM=https://api.typesafe.ai/v1/systemone
#UPSTREAM_TIMEOUT=45
#LIMIT_PER_SEC=1
#LIMIT_PER_HOUR=1000
#LIMIT_PER_DAY=5000
